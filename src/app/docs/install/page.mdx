export const dynamic = 'force-static'

# Установка

Ниже — минимальная установка зависимостей для **Next.js + Nextra** с поддержкой **MDX** и **SSG**.
Логика (список команд для разных пакетных менеджеров) вынесена в `./commands.ts`.

### Предпосылки

- Node.js 18+
- Пакетный менеджер: pnpm / yarn / npm
- TypeScript

### 1) Установка пакетов

Через npm:

```bash
npm i next react react-dom
npm i -D typescript @types/react @types/node
npm i nextra nextra-theme-docs
```

### 2) Проверка MDX-страницы

```mdx file=src/app/page.mdx
export const dynamic = 'force-static'

# Проверка MDX

Документация работает 🚀
```
